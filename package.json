{
  "name": "@oc-soft/wp-color-picker-alpha",
  "version": "0.1.0",
  "description": "wordpress color-picker extention for color alpha. ",
  "main": "dist/wp-color-picker-alpha.js",
  "scripts": {
    "setup-image-resource": "cd image-resource-js && npm install",
    "build-image-resource": "cd image-resource-js && npm run build",
    "build": "rollup -c rollup.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "wordpress",
    "color-picker",
    "alpha"
  ],
  "author": "Toshiyuki Ogawa",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^2.62.0"
  },
  "dependencies": {
    "@rollup/plugin-alias": "^3.1.9"
  }
}
